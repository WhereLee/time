# 块 1 运营闭环收尾 —— 讨论记录（工作草案）

> 状态：**讨论中，未定稿**（v2 2026-09-06：用户业务流程叙述到位，四大结构性决策已拍板；剩余：切块与块 1 正式边界）
> 用途：钉住立项讨论的收敛点，防止发散；定稿后升级为正式计划文档
> 背景：M1 收官（验收 31/31、CI 全绿）后进入下一阶段立项讨论；AI 部分已拍板后置（轻 Agent 全 Java，重 Agent 另立项目）

## 1. 终局定位（已认可）

- 定位：**完善的面试项目**——专注当前业务闭环与 Java 技术深度，AI 后置
- 三根柱子：① 业务闭环健壮性收尾 ② 调度内核治理（用户主战场）③ 触发式压测实证
- 高并发/JVM 姿态：触发式压测"证明与排障"，不伪装高并发
- 分布式：不买第二台服务器（触发条件未到）；集群语义（调度不重复执行）单机双实例即可实证

## 2. 块序列（用户已认可块 2/3，块 1 定稿中）

| 块 | 内容 | 状态 |
|---|---|---|
| 块 1 | 运营闭环收尾：券三件套 + 对账差异管理端可见 + 混合场预置 | 讨论中（本文档） |
| 块 2 | 计费模型真实化：基础免费时长 + 分时电费 + 占位费模式规则 + 单价精度 | 已认可，未立项 |
| 块 3 | 调度治理：集群不重复执行实证/失败重试/告警/执行历史可视化/参数化 | 已认可，未立项 |
| 块 4 | 压测实证（触发式） | 已认可 |
| 横向块 | 原路线 IoT 心跳/Redis 缓存/RocketMQ 消息化/费率引擎 → 降级触发式（处置建议，待用户最终确认） | 待确认 |

## 3. 业务模型讨论收敛（逐决策点）

### 3.1 券制 vs 时长叠加 —— 维持券制（已认可）
- 券制根因 = "停车与充电分属不同管理方"叙事（用户原始想象）：券 = 充电方替车主买单的凭证化表达，现实对应物是商场"充电满 X 送停车券"
- 券的本质 = **留痕数据**：不对用户展示、无券包无码，出场自动核销；BN 码保留为内部对账主键；订单 discount_fen 快照 + 核销记录是用户/管理端可见的一切
- 若改"一家公司"叙事可退化为时长叠加，但会砍掉跨方凭证化（项目最有区分度模块）——不采纳

### 3.2 送券规则三件套（已认可，块 1 落地）
1. **金额门槛**：实付满门槛（5 或 10 元档，**参数化可配置**）才留痕——堵"充 1 Wh 拿券"零成本刷券（M1 现状缺陷：energyWh > 0 即发）
2. **一停车周期一张**：同一锚定停车会话最多一张——堵反复插拔；并发下需条件插入判官（WHERE 不存在该锚定会话可用券）
3. **出场自动核销**：exit 自动找本会话可用留痕核销，用户无感；现状"报码核销"交互退化为自动

### 3.3 快充语境修正（已认可）
- 停车位全是快充/超充（60~250kW），无慢充
- 10 元门槛 ≈ 4~8 分钟 → **门槛的时间约束在快充下失效**，防刷真正靠"免停额度封顶（2h）后正常计费"
- 大电池车充电时长通常 < 1h（快充 0-80% 约 40 分钟内），2h 额度（基础 1h + 充电 1h）绰绰有余
- 模型自洽性与特斯拉"超时占用费"同构：充满不走即开始计费

### 3.4 停车计费模型（用户业务前提，归块 2）
- 用户心智模型 = "首 1 小时免费、超出按小时计费"的免费时长型停车场；当前系统 = 无免费时长纯小时取整
- 归块 2 费率规则改造：fee_rule 加免费时长字段；口径 = 应收按 (时长 − 免费时长) 超出部分小时取整；免费时长内 0 元
- 影响面：FeeCalculator 签名、出场结算、停车单测/IT 金额断言、云上验收金额叙事全变 → 必须赶在任何再验收之前做
- 减免口径定：**先扣时长再对超出取整**（时长口径），非"先算全额再减金额"——2.5h 停 + 1h 免：时长口径收 1h 钱，金额口径收 2h 钱

### 3.5 分时电费（用户逻辑洁癖，归块 2）
- 行业事实：电费分时（尖峰平谷），服务费固定不分时；各省时段划分不同 → 通用时段配置表（任意时段集合），非写死四段
- 技术要点：电量时段归属依赖**桩/电表分时计量**（sim 需升级分段上报，非平台估算）；订单快照从单行变分时明细（恒等叙事延伸到明细级）
- 难度评估：业务计算不难（时间轴与时段求交）；真正设计点在"计量数据来源"与"快照明细表达"

### 3.6 占位费（惩罚性措施，归块 2）
- 两种占位场景：充完不挪车（充电会话结束、停车会话继续）；非充电车占充电位（无充电记录的绑桩车位停车）
- 两种场地模式（规则二选一不写死）：混合停车场 = 停车费本身是惩罚（2h 封顶后自然开收），不叠设备占用费；纯快充站 = 停车不收费，充电结束进宽限期（如 15 分钟）+ 超时按分钟占位费
- 简化决策：占用判定以"车位是否释放"为准（车走没走），**sim 不建模拔枪事件**
- 计数模型无法表达占位费（不知道谁占哪个位）——车位级模型的直接价值反证

### 3.7 计量单位与精度（归块 2）
- 现状：电量 Wh 整数存储、单价 fen/kWh（分/度）计价、显示换算度
- 真边界：真实电价可带小数分（如 53.8 分/度），整数分单价表达不了 → 单价精度扩（厘/度）问题归块 2

### 3.8 车位模型（部分归块 1）
- 模型：park_space 无"是否充电位"字段，**绑桩（charging_pile 1:1）= 充电位**，可选绑定
- 数据缺口（块 1）：云上仅 B-001~003 且全绑桩（02 基线不预置车位、07 只建充电演示）→ 需补**混合场预置**（A 区普通车位 + B 区充电车位），支撑普通位/占位叙事
- 车位级追踪三用途（用户已理解）：设备指令对象、充电锚定载体、占位管理依据

## 4. 现状 vs 待做（M0/M1 已落地能力）

**已有**：会话状态机/车位级追踪/充电锚定（start 锁死 anchor）/两段费率快照/判官并发（入场/充电/出场/核销）/巡检 job（悬挂 2h、长期 3 天、券过期 24h）/对账 job/凭证核销框架/管理端权限隔离
**待做（块 1）**：券三件套改造 + 对账差异管理端可见（M1-1 定稿"管理端可见"未兑现的补账）+ 混合场预置
**待做（块 2）**：免费时长 + 分时电费（sim 分段上报 + 快照明细）+ 占位费模式规则 + 单价精度

## 5. 边界防御清单（面试叙事素材）

防刷：0 电不发（已有）/金额门槛/周期一张/2h 额度封顶超时计费/24h 过期（已有）
并发：双入场/双充电/并发出场/双花核销/同会话并发发券（判官条件更新，前三已有 IT）
时间：0 秒停留/跨小时取整/跨时段电量归属/悬挂与长期占用巡检
金额：整数分/段级取整/快照明细/单价精度
错配：start 车牌一致性、核销锚定校验（已有）

## 6. 待定项

1. 块 1 正式边界与验收标准 —— 讨论记录 v2 后：需按 v2 决策做**切块**（设备域升级/业务模型/开放接口工作量已超出单块，待切）
2. 横向块（IoT 心跳/Redis 缓存/RocketMQ 消息化）降级处置 —— 待用户最终确认（设备在线心跳已因 v2 设备域回需求，范围收敛为台账+在线+sim 心跳）
3. 分时是否进块 2 范围 —— 用户"逻辑洁癖"倾向做，立项时确认
4. 块 1 若含"防重/RepeatSubmit"类组件 —— 挂起中，未定

## 7. 第二轮讨论定稿（2026-09-06，用户业务流程叙述拍板）

### 7.1 四大结构性决策

1. **单一运营方修正（叙事级）**：只设停车方，所有收费归停车方——M1 时代"停车与充电分属不同管理方"叙事根因正式失效。券制代码不推翻（单主体下仍有营销防刷/账目审计价值），但语义从"跨方凭证化"降为"跨域内部凭证 + 内部服务化纪律"；后续文档与面试口径统一为"同一运营方的停车/充电两业务域"，历史块记录不回改。
2. **入场模型 = B 档（自由停放 + 位检绑定）**：入场事件（闸机：车牌+出入口，无车位）→ 建停车会话（新增"待绑定"态）→ 位检事件（车位↔车牌）→ 绑定。入场即建档防位检事件丢失（巡检 job 新增：在场未绑定超时告警）。现状 entry(space,plate) 单事件语义需拆分。
3. **取消电车判定，无充电即占位**：停定充电位 → 宽限内未启动充电 = 占位（警告+宽限+占位费）；充电中豁免；充完新一轮宽限。新增待堵边界：**反复短充规避占位费**（每 8 分钟充 1 分钟重置豁免）→ 豁免只跟随"充电会话进行中"，两次充电间隔超宽限即计费、不回溯豁免。
4. **开放接口形态（不建自营 C 端）**：车牌即身份查询（找车/时长/预计费用/充电位状态）+ 预留车主绑定（第三方小程序接入方定位）+ 无绑定时管理端可见兜底。占位警告渠道：绑定=推送（预留），无绑定=现场告示语义+管理端可见。**特殊支付接口**（查费→模拟支付→状态翻转"已出场+已进场"分段结账）——待堵漏洞：**翻转重置免费时长**（车主分段支付/0 元支付无限续免）→ 新会话继承累计在场时长，免费额度按连续在场计不分段重置。

### 7.2 券与占位费分工（定稿）

- 券核销减免对象 = 停车费（现状实现天然如此，不变）；占位费 = 独立科目，不参与券减免、不占免停额度
- 充完挪普通位继续停 = 位检两次上报（充电位释放、普通位绑定）的自然事件，券在出场核销停车费 ✓ 适用场景闭环
- 收费规则表（充电位）：充电中=豁免 → 充完宽限（繁忙档 5min/闲时 10min，档位配置独立于电价时段）→ 超宽限=占位费（每 10 分钟档）→ 无充电停定=占位宽限后占位费
- 普通位：免费 1h + 按小时 + 券减免（若持券）

### 7.3 用户叙述中的设备域（模拟器扩展需求清单，切块待排）

- 车位 300（绑桩=充电位，比例如 5~10%）；多出入口抬杆；位检设备（自由停放绑定）；充电桩；设备在线状态（心跳回需求，范围=台账+在线+sim 心跳，不碰遥测历史）
- 手动抬杆异常流：需定义操作人/原因/车牌人工录入，会话标记"人工放行"，费用策略显式（防逃费/防漏收，不可"直接归档"了事）
- 占位费"代收划转"落库：结算单/内部划转记录 → 对账 job 新核对组（单运营方下为部门间核算）
- sim 批量设备清单生成（300 车位配置化，不手写）；DB 300 车位预置脚本与 CI 挂载、云上资产重设（影响既有验收脚本叙事）
