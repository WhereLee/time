# M1 计划：充电域（charging）与跨方优惠联动

> 状态：M1-1 设计讨论中（范围已拍板 2026-09-05，见第 3 节）
> 关联：总体计划 v2 M1 行；M0（P1-P6）已于 2026-09-05 全部完成（验收 20/20，commit 238a7f3）

## 1. 目标与验收标准（M1 done 定义）

**目标**：跑通「车停带桩车位 → 充电会话（电量计费：电费+服务费两段费率）→ 充电订单 → 充电方签发免停权益 → 出场结算时停车方凭证化核销减免」跨方联动闭环；Quartz 从空转升级为有业务负载（权益过期/会话超时巡检/跨方对账）；设备模拟扩展充电桩。

验收：
1. 充电闭环 HTTP 可用（桩设备上报开始/结束 → 会话 → 两段费率订单，金额快照正确）
2. 权益全生命周期成立：充电完成签发 → 出场核销减免 → 有效期过期自动作废（调度 job 实证）
3. **同一权益并发核销仅一次成功**（核销判官，复现用例）
4. 停车减免金额正确：减免与取整先后的边界用例（跨小时/超免停时长/零时长）全绿
5. 管理端权限隔离：充电菜单（3xx）与停车菜单（2xx）分角色可查，越权拒绝
6. 全部单测绿（存量 83 + 新增）+ CI 绿（04 脚本挂载容器）
7. 云服务器部署后 M1 验收清单实跑全过（含调度 job 日志实证 + 重启恢复）

## 2. 范围边界

**做**：充电桩台账（绑车位，不重建空间模型）；充电会话（开始/结束/取消 + 判官）；电量计费（固定电费单价 + 服务费，两段快照）；免停权益（凭证化签发/核销/过期状态机）；停车结算减免改造（FeeCalculator 扩展 + park_order 快照扩展 + 核销与出场结算双判官同事务）；Quartz 业务 job（权益过期/充电会话超时巡检/长期占用巡检/跨方对账）；device-sim 充电桩扩展（事件 + 总电量上报）；充电管理端最小面（桩/会话/订单/权益查询）；授权脚本 04 固化。

**不做（后续块）**：支付语义（订单无支付状态，M0 同构）；峰谷分时电价（费率引擎 M2）；遥测实时存储与心跳/在线状态（M2 IoT 线）；充电桩独立进程/独立库（演进拆分条件未到，见决策记录）；优惠叠加/多规则（先单张核销）；停车支付补全。

## 3. 模型与决策记录（2026-09-05 已确认）

| 决策 | 内容 | 理由链 |
|---|---|---|
| 跨方形态 = 同进程 + **凭证化协议边界**（岔路 C） | 不拆 charging 进程；parking/charging 各自自治，跨域不共享表直连：charging 签发权益（凭证实体：幂等键/有效期/状态机），parking 出场**凭凭证核销**（条件更新判官防双花），按"不可信对方"设计接口（防重放/幂等/对账视角） | 用户原始叙事（不同管理方 + 充电免停）的技术化表达；伪微服务（同认证/同库/单机部署的进程切）是负资产：无独立伸缩/部署收益却背分布式成本；进程边界不产生信任边界——信任边界靠协议设计；演进拆分条件未到（详见总体计划） |
| 空间模型共享 | 车位主数据仍在 parking（park_space）；charging 新增 `charging_pile` 绑 `space_id`，**不重建空间表** | 车位是共享物理资源；避免两套空间 CRUD 的体积重复；桩→车位→停车会话的锚定链自然成立 |
| 计费各域自治、范式统一 | charging 自持计费纯函数（电量×电费单价+服务费），不抄 FeeCalculator 代码；范式继承：纯整数分存储/纯函数/边界防御/单测矩阵 | 时长计费与电量计费不同构（领域本质）；跨上下文共享内核破坏限界上下文自治；范式复用不是代码复用 |
| 免停权益 = 1 张 | 单权益单次核销；有效期制；减免语义与取整先后在 M1-1 拍板 | M1 窄而深；叠加/多张规则 M2 优惠引擎 |
| 调度 = M1 真需求 | 4 job：权益过期作废/充电会话超时巡检/长期占用巡检/跨方对账 | 权益过期纯时间驱动（无调度即永不失效）；充电会话悬挂需巡检；跨方协作必然对账——Quartz 从空转转正，触发壳子 Quartz 治理升级项（roadmap 表） |
| 支付语义不进 M1 | 订单"生成即完结"（M0 同构） | 停车 M0 已免支付；充电补支付需同步补停车支付，范围爆炸；对账仍是真实需求（跨方对账 ≠ 支付对账） |

## 4. 领域对象草案（字段级 M1-1 讨论定稿）

- `charging_pile` 充电桩：id、桩编号(唯一)、绑定车位 id（park_space 外键语义，不建 DB 外键）、状态(空闲/充电中/离线占位?)、创建信息
- `charge_session` 充电会话：id、桩 id、车位 id、车牌号、开始时间、结束时间、状态(充电中/已结束/已取消)、总电量（Wh?）、取消原因
- `charge_order` 充电订单：id、会话 id(唯一)、快照字段（桩/车位/车牌/开始/结束/电量/电费单价/服务费单价/电费金额/服务费金额/总金额分）、状态(已生成)、创建时间
- `benefit_record` 免停权益：id、权益码(唯一/幂等键)、来源充电订单 id、车牌号、锚定停车会话(可空，核销时写)、免停时长(秒/分)、有效期止、状态(可用/已核销/已过期)、核销订单 id、签发/核销/过期时间
- `park_order` 扩展：+减免时长字段（或减免金额）、权益码快照、实付金额语义（M1-1 定：减免在取整前还是后直接影响字段语义）
- `fee_rule` 扩展 or 新费率表：充电两段费率（电费单价 fen/kWh、服务费单价 fen/kWh）→ M1-1 定表形态

状态机（充电会话）：`充电中 → 已结束 | 已取消`（终态不可逆，判官同 parking）
状态机（权益）：`可用 → 已核销 | 已过期`（核销=条件更新判官；过期=调度 job 条件更新）

## 5. 接口面草案

管理端（权限串前缀 `charge:xxx`、`park:order/benefit`，注册 sys_menu 3xx）：
- 桩列表/绑定；充电会话/充电订单/权益记录分页查询（只读最小面）

设备/开放端（X-Device-Token 通道扩展 `/device/charging/*` 或复用现有通道语义）：
- `POST /device/charging/start`（桩编号/车位/车牌 → 充电会话，若该车位停车中则自动锚定停车会话）
- `POST /device/charging/finish`（会话 → 电量上报 → 订单 + 权益签发）
- `POST /device/charging/cancel`（充电中止撤销）

device-sim：新增 PILE-001~003 绑 B-001~003 车位 + 手控事件（start/finish/cancel，finish 携带总电量）+ 充电剧本。

调度（Quartz）：权益过期扫描（每分钟）、充电会话超时巡检（断连悬挂强制终态+算费）、长期占用巡检（报警/标记）、跨方对账（签发 vs 核销差异清单，日志+管理端可见）。

## 6. M1 小步划分（每步走块循环 + 自动提交）

| 步 | 内容 | 产出/验收 |
|---|---|---|
| M1-1 | 设计讨论定稿：四表字段级 + 状态机 + 减免算法（取整先后）+ 凭证协议（接口语义/幂等键）+ 费率表形态 | 本文档更新为定稿版 |
| M1-2 | DDL（04-charging.sql：pile/session/order/benefit + park_order 减免字段 ALTER + sys_menu 3xx + 04 授权脚本幂等）+ entity/dao + CI 挂载 | 表结构落地、实体/Mapper、CI 容器 4 文件全绿 |
| M1-3 | charging 会话与桩服务（状态机 + 判官 + 电量累计口径） | 核心单测（并发/非法迁移/重复开始） |
| M1-4 | charging 结算（两段费率纯函数 + 订单快照 + 权益签发） | 计费边界用例（零电量/跨段/大数值防溢出） |
| M1-5 | 停车减免改造（FeeCalculator 扩展 + park_order 快照 + 出场核销双判官同事务） | 减免边界用例 + ParkSessionIT 扩展（含权益核销并发） |
| M1-6 | 调度 job ×4（Quartz 治理升级随块落地） | job 单测 + 手工触发实证 |
| M1-7 | device-sim 充电桩扩展 + 双进程联调 | 剧本/手控实证（sim 日志） |
| M1-8 | 管理端 HTTP + 权限（3xx 菜单 + 授权） | HTTP 冒烟 + 越权拒绝实证 |
| M1-9 | 云上部署 + M1 验收清单实跑 + 收口文档 | M1 验收全过 |

穿插项（小块独立）：登录重试守卫抽取（roadmap 触发表已挂）。

## 7. 市场调研结论（2026-09-05，本次立项依据）

1. 2026 Java 简历池：电商/秒杀/商城/权限订单后台为"烂大街"，项目标题拉不开差距；停车场 CRUD 版已是毕设模板——**深度是唯一区分器**（面试官三层模型：功能/设计/工程层；追问三轮暴露短板）
2. 一致性场景题标准答案要素：状态机/条件更新/幂等/扫描补偿/对账——M0 已落前三，**M1 补齐扫描补偿（巡检 job）与对账**
3. 差异化方向点名：物联网设备数据采集与监控（充电运营平台有真实政企项目形态背书）；已云部署可挂链接为实证加分
4. 校准出的三个缺口（后续阶段补齐）：IoT 心跳/在线状态/遥测（M2）；Redis 业务缓存场景（M2）；RocketMQ 可靠投递（M3）
5. 模块化单体是正派架构（开源高质量项目同构），且有明确演进拆分判据

## 8. 后续阶段路线（衔接总方案）

| 阶段 | 内容 | 触发/状态 |
|---|---|---|
| M1（本计划） | charging 域 + 跨方优惠联动 + 调度转正 | 进行中 |
| M2 | IoT 能力补全：设备心跳/在线状态/遥测存储（充电电量即遥测）+ Redis 业务缓存（车位/桩状态）+ MQTT 演进判断 | M1 后 |
| M3 | 消息化：RocketMQ 事务消息承载权益签发/停车事件（可靠投递/重复消费） | M2 后 |
| M4 | 压测与演进验证：JMeter/arthas/Prometheus（触发式）→ 演进拆分决策实证 | 触发条件式 |
| AI | 对账差异解释 Agent / ChatBI | 最后期 |

## 9. M1-1 讨论议程（待拍板问题）

1. 四表字段级定稿（电量单位 Wh/分存储口径、权益免停时长单位、费率表形态：扩展 fee_rule vs 新表）
2. 权益锚定：桩→车位→停车会话（充电时该车位无停车会话则只发权益不锚定，出场按车牌匹配？）
3. 减免算法：减免在取整前/后；超免停部分计费口径；park_order 新增字段语义（减免时长 vs 减免金额 vs 双记）
4. 凭证协议细节：权益码生成（UUID/业务号）、核销幂等键、签发与核销接口归属（parking 侧 service 直接查 benefit 表 vs 跨上下文 service 调用）
5. 调度参数与 Quartz 治理升级范围（misfire/重试/告警做到什么程度）
